# Лендинг moongatracker — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Собрать маркетинговый лендинг moongatracker как новый Nx-апп `apps/landing` (Vite+React): каркас смены полноэкранных карточек — из референса `~/ai-reviews-landing`, стиль (токены + JetBrains Mono) — из `apps/web`, контент — под SaaS-продукт для разработчиков с AI-агентами, CTA ведёт на регистрацию.

**Architecture:** Отдельный Nx React/Vite апп. Каркас (`StickyCardStack`, `ScrollNav`, `AnimatedBeam`) портируется из референса с заменой Next-специфики (`next/link` → `<a>`, шрифт через CSS `@import`). Пять полноэкранных карточек: Hero → Как это работает → Интерактивное демо → Хаб интеграций → Тарифы/CTA. Логика демо — детерминированный клиентский модуль (без бэкенда/LLM), покрыт тестами. Тема — копия oklch-токенов и шрифта из `apps/web/src/styles.css` (в т.ч. `--radius: 0`).

**Tech Stack:** Nx 23, Vite, React 19, Tailwind v4, shadcn/ui, GSAP + ScrollTrigger, `@gsap/react`, Lenis (`lenis/react`), `motion`, `lucide-react`, `react-use-measure`, vitest + @testing-library/react (jsdom).

**Источники для копирования (та же машина):**
- Референс каркаса: `/Users/dmitrijordin/ai-reviews-landing/src/`
- Тема/шрифт: `/Users/dmitrijordin/moongatracker/apps/web/src/styles.css`
- UI-примитивы: `/Users/dmitrijordin/moongatracker/apps/web/src/components/ui/{button,textarea}.tsx`
- Дизайн-спека: `docs/superpowers/specs/2026-07-05-landing-design.md`

**Константы контента:**
- CTA регистрации → `APP_URL` = `import.meta.env.VITE_APP_URL ?? '/register'`.
- Узлы хаба: Claude · OpenClaw (Telegram) · MCP-сервер · REST API · n8n · Socket.IO · API-токены.
- Тарифы на старте без цен: Free / Pro / Team.

---

## File Structure (apps/landing/src)

```
apps/landing/
├── index.html
├── vite.config.ts                 # + alias '@' -> ./src
├── project.json                    # nx targets (build/serve/test)
├── tsconfig*.json                  # + paths '@/*' -> ./src/*
└── src/
    ├── main.tsx                    # bootstrap
    ├── styles.css                  # копия токенов из apps/web + JetBrains Mono
    ├── app/
    │   └── app.tsx                 # композиция: Header + стек 5 карточек
    ├── lib/
    │   ├── utils.ts                # cn()
    │   ├── constants.ts            # APP_URL
    │   ├── agent-demo.ts           # детерминированная логика демо (TESTED)
    │   └── agent-demo.spec.ts
    ├── components/
    │   ├── ui/
    │   │   ├── button.tsx          # копия из apps/web
    │   │   └── textarea.tsx        # копия из apps/web
    │   ├── sticky-card-stack.tsx   # порт из референса
    │   ├── scroll-nav.tsx          # порт из референса
    │   ├── animated-beam.tsx       # порт из референса (magicui)
    │   ├── logo.tsx
    │   ├── header.tsx
    │   ├── hero-section.tsx        # карточка 1
    │   ├── how-it-works-section.tsx# карточка 2
    │   ├── demo-section.tsx        # карточка 3
    │   ├── integrations-card.tsx   # карточка 4
    │   └── pricing-section.tsx     # карточка 5
    └── (spec-файлы рядом с компонентами карточек)
```

---

## Task 1: Scaffold `apps/landing` + зависимости

**Files:**
- Create: `apps/landing/*` (генератор Nx)
- Modify: `apps/landing/vite.config.ts`, `apps/landing/tsconfig.json`

- [ ] **Step 1: Сгенерировать Nx React/Vite апп**

Сначала свериться с генератором через skill `nx-generate` (правило репозитория). Затем:

```bash
npx nx g @nx/react:application landing \
  --directory=apps/landing \
  --bundler=vite \
  --style=css \
  --routing=false \
  --unitTestRunner=vitest \
  --e2eTestRunner=none \
  --no-interactive
```

Ожидаемо: создан `apps/landing` с `vite.config.ts`, `project.json` (targets build/serve/test), заготовка `src/app/app.tsx`, `src/main.tsx`.

- [ ] **Step 2: Установить рантайм-зависимости лендинга**

```bash
npm install gsap@^3.15.0 @gsap/react@^2.1.2 lenis@^1.3.25 motion@^12 \
  react-use-measure@^2.1.7 lucide-react class-variance-authority clsx tailwind-merge \
  tw-animate-css @fontsource-variable/jetbrains-mono
```

Ожидаемо: пакеты добавлены в корневой `package.json` (монорепо, один node_modules).

- [ ] **Step 3: Добавить alias `@` → `src` в vite.config.ts**

В `apps/landing/vite.config.ts` в объект конфигурации добавить (внутри `defineConfig({...})`):

```ts
import * as path from 'path';
// ...
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src'),
    },
  },
```

- [ ] **Step 4: Добавить paths `@/*` в tsconfig**

В `apps/landing/tsconfig.json` (или `tsconfig.app.json`, куда смотрит апп) в `compilerOptions`:

```json
"baseUrl": ".",
"paths": { "@/*": ["src/*"] }
```

- [ ] **Step 5: Проверить, что апп собирается**

Run: `npx nx build landing`
Expected: успешная сборка заготовки (dist создан, без ошибок).

- [ ] **Step 6: Commit**

```bash
git add apps/landing package.json package-lock.json nx.json tsconfig.base.json 2>/dev/null
git commit -m "feat(landing): scaffold Nx vite app + deps"
```

---

## Task 2: Тема — токены moongatracker + JetBrains Mono

**Files:**
- Modify/Create: `apps/landing/src/styles.css`
- Modify: `apps/landing/index.html` (lang="ru"), `apps/landing/src/main.tsx` (импорт styles.css)

- [ ] **Step 1: Заменить содержимое `apps/landing/src/styles.css`**

Скопировать ровно содержимое `apps/web/src/styles.css` (строки 1–131: `@theme inline`, `:root`, `.dark`, `@layer base`). Это включает `@import "@fontsource-variable/jetbrains-mono"`, `--font-heading`, `--font-mono`, все oklch-токены (primary `oklch(0.514 0.222 16.935)`), `--radius: 0` и `html { @apply font-mono }`.

Если строка `@import "shadcn/tailwind.css";` вызывает ошибку резолва в этом аппе — заменить её на прямой импорт tailwind (`@import "tailwindcss";` уже присутствует первой строкой) и удалить строку shadcn-импорта. Проверяется в Step 4.

- [ ] **Step 2: Убедиться, что styles.css импортируется в `main.tsx`**

В `apps/landing/src/main.tsx` первой строкой импортов:

```ts
import './styles.css';
```

- [ ] **Step 3: Выставить язык страницы**

В `apps/landing/index.html` заменить `<html lang="en">` на `<html lang="ru">`, а `<title>` на `moongatracker — канбан, где AI-агент участник`.

- [ ] **Step 4: Проверить сборку/dev**

Run: `npx nx build landing`
Expected: сборка успешна, CSS с токенами включён (без ошибок импорта).

- [ ] **Step 5: Commit**

```bash
git add apps/landing/src/styles.css apps/landing/src/main.tsx apps/landing/index.html
git commit -m "feat(landing): moongatracker theme tokens + JetBrains Mono"
```

---

## Task 3: UI-примитивы + утилиты

**Files:**
- Create: `apps/landing/src/lib/utils.ts`, `apps/landing/src/lib/constants.ts`
- Create: `apps/landing/src/components/ui/button.tsx`, `apps/landing/src/components/ui/textarea.tsx`

- [ ] **Step 1: Создать `apps/landing/src/lib/utils.ts`**

```ts
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

- [ ] **Step 2: Создать `apps/landing/src/lib/constants.ts`**

```ts
// Куда ведут CTA регистрации. В проде переопределяется через VITE_APP_URL.
export const APP_URL: string =
  (import.meta.env.VITE_APP_URL as string | undefined) ?? '/register';
```

- [ ] **Step 3: Скопировать UI-примитивы из apps/web**

Скопировать без изменений:
- `apps/web/src/components/ui/button.tsx` → `apps/landing/src/components/ui/button.tsx`
- `apps/web/src/components/ui/textarea.tsx` → `apps/landing/src/components/ui/textarea.tsx`

Убедиться, что их импорт `@/lib/utils` резолвится (alias из Task 1). Если в этих файлах есть импорт иных `@/*` модулей, которых нет в лендинге, — заменить на локальные или удалить.

- [ ] **Step 4: Проверить типы**

Run: `npx nx build landing`
Expected: сборка успешна (button/textarea компилируются).

- [ ] **Step 5: Commit**

```bash
git add apps/landing/src/lib apps/landing/src/components/ui
git commit -m "feat(landing): cn util, APP_URL, shadcn button/textarea"
```

---

## Task 4: Каркас — StickyCardStack + ScrollNav (порт из референса)

**Files:**
- Create: `apps/landing/src/components/sticky-card-stack.tsx`
- Create: `apps/landing/src/components/scroll-nav.tsx`

- [ ] **Step 1: Скопировать `sticky-card-stack.tsx` из референса**

Скопировать `/Users/dmitrijordin/ai-reviews-landing/src/components/sticky-card-stack.tsx` → `apps/landing/src/components/sticky-card-stack.tsx` **без изменений** (файл уже фреймворк-нейтрален: использует `@gsap/react`, `gsap/ScrollTrigger`, `lenis/react`, `@/lib/utils` — всё работает в Vite). Удалить только директиву `"use client";` в первой строке (в Vite она не нужна; оставлять безвредно, но убираем для чистоты).

- [ ] **Step 2: Скопировать `scroll-nav.tsx` из референса**

Скопировать `/Users/dmitrijordin/ai-reviews-landing/src/components/scroll-nav.tsx` → `apps/landing/src/components/scroll-nav.tsx`. Удалить `"use client";`. Импорты (`lenis/react`, `lucide-react`, `@/lib/utils`) — без изменений.

- [ ] **Step 3: Временная проверочная композиция**

Заменить `apps/landing/src/app/app.tsx` на минимальную проверку каркаса:

```tsx
import ReactLenis from 'lenis/react';
import { StickyCardStack } from '@/components/sticky-card-stack';

export function App() {
  return (
    <ReactLenis root options={{ autoRaf: false, smoothWheel: false }}>
      <StickyCardStack>
        <div className="flex h-full w-full items-center justify-center bg-background text-4xl">1</div>
        <div className="flex h-full w-full items-center justify-center bg-card text-4xl">2</div>
      </StickyCardStack>
    </ReactLenis>
  );
}
export default App;
```

- [ ] **Step 4: Проверить в dev-режиме, что смена карточек работает**

Run: `npx nx serve landing`
Expected: открывается `http://localhost:4200` (или порт из project.json), колесо/стрелки/свайп переключают экран «1» → «2» одним жестом, скролл запинен.

- [ ] **Step 5: Commit**

```bash
git add apps/landing/src/components/sticky-card-stack.tsx apps/landing/src/components/scroll-nav.tsx apps/landing/src/app/app.tsx
git commit -m "feat(landing): port sticky-card-stack + scroll-nav skeleton"
```

---

## Task 5: Header + Logo

**Files:**
- Create: `apps/landing/src/components/logo.tsx`
- Create: `apps/landing/src/components/header.tsx`
- Test: `apps/landing/src/components/header.spec.tsx`

- [ ] **Step 1: Написать падающий тест хедера**

`apps/landing/src/components/header.spec.tsx`:

```tsx
import { render, screen } from '@testing-library/react';
import { Header } from './header';

describe('Header', () => {
  it('показывает бренд и CTA регистрации', () => {
    render(<Header />);
    expect(screen.getByText(/moongatracker/i)).toBeTruthy();
    const cta = screen.getByRole('link', { name: /начать бесплатно/i });
    expect(cta.getAttribute('href')).toContain('/register');
  });
});
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `npx nx test landing`
Expected: FAIL (модуль `./header` не найден).

- [ ] **Step 3: Создать `logo.tsx`**

```tsx
import { Bot } from 'lucide-react';

export function Logo() {
  return (
    <span className="flex items-center gap-2 font-heading font-semibold">
      <Bot className="size-5 text-primary" />
      <span>moongatracker</span>
    </span>
  );
}
```

- [ ] **Step 4: Создать `header.tsx`**

```tsx
import { useEffect, useState } from 'react';
import { useScroll, motion } from 'motion/react';
import { Menu, X } from 'lucide-react';
import { Logo } from '@/components/logo';
import { Button } from '@/components/ui/button';
import { cn } from '@/lib/utils';
import { APP_URL } from '@/lib/constants';

const menuItems = [
  { name: 'Как работает', href: '#how' },
  { name: 'Демо', href: '#demo' },
  { name: 'Интеграции', href: '#integrations' },
  { name: 'Тарифы', href: '#pricing' },
];

export function Header() {
  const [menuState, setMenuState] = useState(false);
  const [scrolled, setScrolled] = useState(false);
  const { scrollYProgress } = useScroll();

  useEffect(() => {
    const unsub = scrollYProgress.on('change', (v) => setScrolled(v > 0.05));
    return () => unsub();
  }, [scrollYProgress]);

  return (
    <header>
      <nav data-state={menuState && 'active'} className="fixed z-20 w-full pt-2">
        <div className={cn('mx-auto max-w-7xl px-6 transition-all duration-300 lg:px-12', scrolled && 'bg-background/60 backdrop-blur-2xl')}>
          <motion.div className={cn('relative flex flex-wrap items-center justify-between gap-6 py-3 lg:gap-0 lg:py-6', scrolled && 'lg:py-4')}>
            <div className="flex w-full items-center justify-between gap-12 lg:w-auto">
              <a href="/" aria-label="home" className="flex items-center">
                <Logo />
              </a>
              <button
                onClick={() => setMenuState(!menuState)}
                aria-label={menuState ? 'Закрыть меню' : 'Открыть меню'}
                className="relative z-20 -m-2.5 block cursor-pointer p-2.5 lg:hidden"
              >
                <Menu className="in-data-[state=active]:scale-0 in-data-[state=active]:opacity-0 m-auto size-6 duration-200" />
                <X className="in-data-[state=active]:scale-100 in-data-[state=active]:opacity-100 absolute inset-0 m-auto size-6 scale-0 opacity-0 duration-200" />
              </button>
              <div className="hidden lg:block">
                <ul className="flex gap-8 text-sm">
                  {menuItems.map((item) => (
                    <li key={item.href}>
                      <a href={item.href} className="text-muted-foreground hover:text-foreground block duration-150">
                        {item.name}
                      </a>
                    </li>
                  ))}
                </ul>
              </div>
            </div>
            <div className="bg-background in-data-[state=active]:block lg:in-data-[state=active]:flex mb-6 hidden w-full flex-wrap items-center justify-end space-y-8 border p-6 shadow-2xl md:flex-nowrap lg:m-0 lg:flex lg:w-fit lg:gap-6 lg:space-y-0 lg:border-transparent lg:bg-transparent lg:p-0 lg:shadow-none">
              <div className="lg:hidden">
                <ul className="space-y-6 text-base">
                  {menuItems.map((item) => (
                    <li key={item.href}>
                      <a href={item.href} className="text-muted-foreground hover:text-foreground block duration-150">
                        {item.name}
                      </a>
                    </li>
                  ))}
                </ul>
              </div>
              <div className="flex w-full flex-col space-y-3 sm:flex-row sm:gap-3 sm:space-y-0 md:w-fit">
                <Button asChild variant="outline" size="sm">
                  <a href={APP_URL}>Войти</a>
                </Button>
                <Button asChild size="sm">
                  <a href={APP_URL}>Начать бесплатно</a>
                </Button>
              </div>
            </div>
          </motion.div>
        </div>
      </nav>
    </header>
  );
}
```

- [ ] **Step 5: Запустить тест — убедиться, что проходит**

Run: `npx nx test landing`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add apps/landing/src/components/logo.tsx apps/landing/src/components/header.tsx apps/landing/src/components/header.spec.tsx
git commit -m "feat(landing): header + logo"
```

---

## Task 6: Карточка 1 — Hero

**Files:**
- Create: `apps/landing/src/components/hero-section.tsx`
- Test: `apps/landing/src/components/hero-section.spec.tsx`

- [ ] **Step 1: Написать падающий тест**

`apps/landing/src/components/hero-section.spec.tsx`:

```tsx
import { render, screen } from '@testing-library/react';
import { HeroCard } from './hero-section';

describe('HeroCard', () => {
  it('показывает H1 про агента-участника и CTA на регистрацию', () => {
    render(<HeroCard />);
    expect(screen.getByRole('heading', { level: 1 }).textContent).toMatch(/полноправный участник/i);
    const cta = screen.getByRole('link', { name: /начать бесплатно/i });
    expect(cta.getAttribute('href')).toContain('/register');
  });
});
```

- [ ] **Step 2: Запустить — падает**

Run: `npx nx test landing`
Expected: FAIL (нет `./hero-section`).

- [ ] **Step 3: Создать `hero-section.tsx`**

```tsx
import { ChevronRight } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { ScrollNav } from '@/components/scroll-nav';
import { APP_URL } from '@/lib/constants';

export function HeroCard() {
  return (
    <div className="relative flex h-full w-full items-center justify-center overflow-hidden bg-background">
      {/* Слот фоновой анимации (визуалка — из стиля moongatracker). Мягкое свечение primary. */}
      <div className="pointer-events-none absolute inset-0 bg-[radial-gradient(60%_50%_at_50%_35%,color-mix(in_oklch,var(--primary)_18%,transparent),transparent)]" />

      <div className="relative z-10 mx-auto flex max-w-4xl flex-col items-center px-6 text-center">
        <span className="mb-5 inline-flex items-center gap-2 border border-border px-3 py-1 text-xs text-muted-foreground">
          канбан для человека и AI-агента
        </span>
        <h1 className="text-balance font-heading text-4xl font-semibold tracking-tight sm:text-5xl md:text-6xl">
          Канбан, где AI-агент — полноправный участник
        </h1>
        <p className="mt-5 max-w-xl text-balance text-lg text-muted-foreground">
          Идея живёт на одной карточке — от наброска до релиза. Агент читает, создаёт и двигает
          карточки через MCP, а ты видишь каждый его шаг и в один клик откатываешь.
        </p>
        <div className="mt-8 flex flex-col items-center gap-3 sm:flex-row">
          <Button asChild size="lg" className="h-12 pl-5 pr-3 text-base">
            <a href={APP_URL}>
              <span className="text-nowrap">Начать бесплатно</span>
              <ChevronRight className="ml-1" />
            </a>
          </Button>
          <Button asChild size="lg" variant="ghost" className="h-12 px-5 text-base">
            <a href="#demo"><span className="text-nowrap">Смотреть демо</span></a>
          </Button>
        </div>
        <ScrollNav dir="down" label="Листайте вниз" className="mt-12" />
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Запустить — проходит**

Run: `npx nx test landing`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add apps/landing/src/components/hero-section.tsx apps/landing/src/components/hero-section.spec.tsx
git commit -m "feat(landing): hero card"
```

---

## Task 7: Карточка 2 — Как это работает

**Files:**
- Create: `apps/landing/src/components/how-it-works-section.tsx`
- Test: `apps/landing/src/components/how-it-works-section.spec.tsx`

- [ ] **Step 1: Написать падающий тест**

`apps/landing/src/components/how-it-works-section.spec.tsx`:

```tsx
import { render, screen } from '@testing-library/react';
import { HowItWorksCard } from './how-it-works-section';

describe('HowItWorksCard', () => {
  it('показывает 4 шага пути идеи', () => {
    render(<HowItWorksCard />);
    expect(screen.getByRole('heading', { level: 2 }).textContent).toMatch(/от идеи до релиза/i);
    expect(screen.getAllByTestId('how-step')).toHaveLength(4);
  });
});
```

- [ ] **Step 2: Запустить — падает**

Run: `npx nx test landing`
Expected: FAIL.

- [ ] **Step 3: Создать `how-it-works-section.tsx`**

```tsx
import { Lightbulb, Search, GitBranch, CheckCircle2 } from 'lucide-react';
import { ScrollNav } from '@/components/scroll-nav';

const STEPS = [
  { icon: Lightbulb, title: 'Кидаешь идею', text: 'В чат агенту (Telegram / Claude) или прямо на доску.' },
  { icon: Search, title: 'Агент разбирает', text: 'Вопросы, ресёрч, оценка, лейблы → двигает в «Разбор».' },
  { icon: GitBranch, title: '«Берём»', text: 'Карточка уходит в разработку, агент дописывает мини-спеку в тело.' },
  { icon: CheckCircle2, title: 'Ведётся до «Готово»', text: 'Обсуждение и вся история остаются на карточке.' },
];

export function HowItWorksCard() {
  return (
    <div id="how" className="relative flex h-full w-full items-center overflow-y-auto bg-card px-6 py-16">
      <ScrollNav dir="up" label="Назад" className="absolute inset-x-0 top-24 z-20 mx-auto w-fit" />
      <div data-fade-on-scroll className="mx-auto w-full max-w-4xl">
        <div className="mx-auto mb-12 max-w-2xl text-center">
          <h2 className="text-balance font-heading text-3xl font-semibold tracking-tight md:text-4xl">
            От идеи до релиза — на одной карточке
          </h2>
          <p className="mt-3 text-balance text-muted-foreground">
            Один путь <code className="text-foreground">idea → разбор → backlog → in dev → done</code>,
            два вида: «Идеи» и «Разработка».
          </p>
        </div>
        <ol className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
          {STEPS.map((s, i) => (
            <li key={s.title} data-testid="how-step" className="border border-border bg-background p-5">
              <div className="flex items-center gap-2 text-primary">
                <s.icon className="size-5" />
                <span className="text-xs text-muted-foreground">Шаг {i + 1}</span>
              </div>
              <div className="mt-3 font-heading font-semibold">{s.title}</div>
              <p className="mt-1 text-sm text-muted-foreground">{s.text}</p>
            </li>
          ))}
        </ol>
      </div>
      <ScrollNav dir="down" label="Листайте вниз" className="absolute inset-x-0 bottom-6 z-20 mx-auto w-fit" />
    </div>
  );
}
```

- [ ] **Step 4: Запустить — проходит**

Run: `npx nx test landing`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add apps/landing/src/components/how-it-works-section.tsx apps/landing/src/components/how-it-works-section.spec.tsx
git commit -m "feat(landing): how-it-works card"
```

---

## Task 8: Логика демо (детерминированный «агент») — TDD

**Files:**
- Create: `apps/landing/src/lib/agent-demo.ts`
- Test: `apps/landing/src/lib/agent-demo.spec.ts`

- [ ] **Step 1: Написать падающие тесты**

`apps/landing/src/lib/agent-demo.spec.ts`:

```ts
import { buildAgentRun, sizeOf } from './agent-demo';

describe('agent-demo', () => {
  it('навешивает лейбл ui на идею про тему/интерфейс', () => {
    const run = buildAgentRun('Тёмная тема для настроек');
    expect(run.labels).toContain('ui');
    expect(run.from).toBe('idea');
    expect(run.to).toBe('разбор');
    expect(run.comment.length).toBeGreaterThan(0);
  });

  it('навешивает лейбл интеграции на идею про вебхук/telegram', () => {
    const run = buildAgentRun('Вебхук в Telegram на смену статуса');
    expect(run.labels).toContain('интеграции');
  });

  it('всегда добавляет лейбл оценки размера', () => {
    const run = buildAgentRun('Экспорт доски в Markdown');
    expect(run.labels.some((l) => l.startsWith('оценка:'))).toBe(true);
  });

  it('детерминирован: одинаковый вход → одинаковый выход', () => {
    expect(buildAgentRun('Экспорт в Markdown')).toEqual(buildAgentRun('Экспорт в Markdown'));
  });

  it('sizeOf растёт с длиной', () => {
    expect(sizeOf('коротко')).toBe('S');
    expect(sizeOf('a'.repeat(120))).toBe('L');
  });
});
```

- [ ] **Step 2: Запустить — падает**

Run: `npx nx test landing`
Expected: FAIL (нет `./agent-demo`).

- [ ] **Step 3: Реализовать `agent-demo.ts`**

```ts
export type ColumnKey = 'idea' | 'разбор';

export interface AgentRun {
  comment: string;
  labels: string[];
  from: ColumnKey;
  to: ColumnKey;
}

const RULES: { label: string; words: string[] }[] = [
  { label: 'ui', words: ['тём', 'тема', 'интерфейс', 'ui', 'экран', 'кнопк'] },
  { label: 'интеграции', words: ['вебхук', 'webhook', 'telegram', 'уведомл', 'api', 'mcp', 'бот'] },
  { label: 'экспорт', words: ['экспорт', 'markdown', 'выгруз', 'csv', 'импорт'] },
  { label: 'bug', words: ['баг', 'фикс', 'ошибк', 'падает', 'не работает'] },
];

export function sizeOf(text: string): 'S' | 'M' | 'L' {
  const n = text.trim().length;
  if (n <= 40) return 'S';
  if (n <= 90) return 'M';
  return 'L';
}

export function detectLabels(text: string): string[] {
  const t = text.toLowerCase();
  const hits = RULES.filter((r) => r.words.some((w) => t.includes(w))).map((r) => r.label);
  return hits.length ? hits : ['фича'];
}

export function buildAgentRun(text: string): AgentRun {
  const size = sizeOf(text);
  const labels = [...detectLabels(text), `оценка: ${size}`];
  const comment =
    `Разобрал идею. Навесил лейблы: ${labels.join(', ')}. ` +
    `Предлагаю оформить критерии приёмки и оценить в ${size}. ` +
    `Двигаю в «Разбор» — подтвердишь перенос в разработку?`;
  return { comment, labels, from: 'idea', to: 'разбор' };
}
```

- [ ] **Step 4: Запустить — проходит**

Run: `npx nx test landing`
Expected: PASS (все 5 тестов).

- [ ] **Step 5: Commit**

```bash
git add apps/landing/src/lib/agent-demo.ts apps/landing/src/lib/agent-demo.spec.ts
git commit -m "feat(landing): deterministic demo agent logic + tests"
```

---

## Task 9: Карточка 3 — Интерактивное демо

**Files:**
- Create: `apps/landing/src/components/demo-section.tsx`
- Test: `apps/landing/src/components/demo-section.spec.tsx`

- [ ] **Step 1: Написать падающий тест поведения**

`apps/landing/src/components/demo-section.spec.tsx`:

```tsx
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { DemoCard } from './demo-section';

describe('DemoCard', () => {
  it('по «Отдать агенту» показывает комментарий агента и перенос карточки, откат возвращает в idea', async () => {
    render(<DemoCard />);
    fireEvent.click(screen.getByRole('button', { name: /отдать агенту/i }));
    await waitFor(() => expect(screen.getByTestId('agent-comment').textContent).toMatch(/разобрал идею/i));
    expect(screen.getByTestId('card-column').textContent).toMatch(/разбор/i);

    fireEvent.click(screen.getByRole('button', { name: /откатить/i }));
    expect(screen.getByTestId('card-column').textContent).toMatch(/idea/i);
  });
});
```

- [ ] **Step 2: Запустить — падает**

Run: `npx nx test landing`
Expected: FAIL.

- [ ] **Step 3: Создать `demo-section.tsx`**

```tsx
import { useEffect, useRef, useState } from 'react';
import { Sparkles, CornerDownRight, RotateCcw } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { ScrollNav } from '@/components/scroll-nav';
import { buildAgentRun, type AgentRun, type ColumnKey } from '@/lib/agent-demo';

const EXAMPLES = [
  'Тёмная тема для настроек',
  'Экспорт доски в Markdown',
  'Вебхук в Telegram на смену статуса',
];

type Phase = 'idle' | 'thinking' | 'typing' | 'done';

export function DemoCard() {
  const [text, setText] = useState(EXAMPLES[2]);
  const [column, setColumn] = useState<ColumnKey>('idea');
  const [run, setRun] = useState<AgentRun | null>(null);
  const [typed, setTyped] = useState('');
  const [phase, setPhase] = useState<Phase>('idle');
  const timers = useRef<ReturnType<typeof setTimeout>[]>([]);

  useEffect(() => () => timers.current.forEach(clearTimeout), []);

  useEffect(() => {
    if (!run) return;
    setTyped('');
    setPhase('typing');
    let i = 0;
    const id = setInterval(() => {
      i += 2;
      setTyped(run.comment.slice(0, i));
      if (i >= run.comment.length) {
        clearInterval(id);
        setPhase('done');
      }
    }, 16);
    return () => clearInterval(id);
  }, [run]);

  function handoff() {
    const result = buildAgentRun(text);
    setPhase('thinking');
    const t = setTimeout(() => {
      setRun(result);
      setColumn(result.to);
    }, 600);
    timers.current.push(t);
  }

  function revert() {
    setRun(null);
    setTyped('');
    setPhase('idle');
    setColumn('idea');
  }

  return (
    <div id="demo" className="relative flex h-full w-full items-center overflow-y-auto bg-background px-6 py-16">
      <ScrollNav dir="up" label="Назад" className="absolute inset-x-0 top-24 z-20 mx-auto w-fit" />
      <div data-fade-on-scroll className="mx-auto w-full max-w-2xl">
        <div className="mx-auto mb-8 max-w-xl text-center">
          <h2 className="text-balance font-heading text-3xl font-semibold tracking-tight md:text-4xl">
            Посмотри, как агент ведёт карточку
          </h2>
        </div>

        <div className="border border-border bg-card">
          <div className="p-5 sm:p-6">
            {/* Шапка карточки: статус-колонка */}
            <div className="flex items-center justify-between">
              <span className="text-xs text-muted-foreground">Карточка · колонка:</span>
              <span data-testid="card-column" className="border border-border px-2 py-0.5 text-xs font-medium text-primary">
                {column}
              </span>
            </div>

            <textarea
              value={text}
              onChange={(e) => setText(e.target.value)}
              rows={2}
              placeholder="Опиши идею…"
              className="mt-3 w-full resize-none bg-transparent text-[15px] leading-relaxed outline-none placeholder:text-muted-foreground"
            />

            <div className="mt-1 flex flex-wrap gap-1.5">
              {EXAMPLES.map((ex, i) => (
                <button
                  key={ex}
                  type="button"
                  onClick={() => { setText(ex); revert(); }}
                  className="border border-border px-2.5 py-1 text-xs text-muted-foreground transition-colors hover:text-foreground"
                >
                  Пример {i + 1}
                </button>
              ))}
            </div>

            {phase !== 'idle' && (
              <div className="mt-5 border-t border-border pt-5">
                <div className="bg-primary/[0.06] p-4">
                  <div className="flex items-center gap-2">
                    <div className="flex size-7 items-center justify-center bg-primary text-xs font-semibold text-primary-foreground">A</div>
                    <span className="text-sm font-medium">Агент</span>
                    <span className="text-xs text-muted-foreground">· комментарий</span>
                  </div>
                  <p data-testid="agent-comment" className="mt-2 whitespace-pre-line text-sm leading-relaxed text-foreground/90">
                    {phase === 'thinking' ? (
                      <span className="inline-flex items-center gap-1.5 text-muted-foreground">
                        <Sparkles className="size-3.5 animate-pulse" /> Агент думает…
                      </span>
                    ) : (
                      <>
                        {typed}
                        {phase === 'typing' && <span className="ml-0.5 inline-block w-1.5 animate-pulse">▍</span>}
                      </>
                    )}
                  </p>
                  {run && phase === 'done' && (
                    <div className="mt-3 flex flex-wrap gap-1.5">
                      {run.labels.map((l) => (
                        <span key={l} className="border border-border px-2 py-0.5 text-xs text-muted-foreground">{l}</span>
                      ))}
                    </div>
                  )}
                </div>

                {run && phase === 'done' && (
                  <div className="mt-3 flex items-center justify-between text-xs text-muted-foreground">
                    <span>агент переместил карточку: {run.from} → {run.to}</span>
                    <button type="button" onClick={revert} className="inline-flex items-center gap-1 hover:text-foreground">
                      <RotateCcw className="size-3.5" /> Откатить
                    </button>
                  </div>
                )}
              </div>
            )}
          </div>

          <div className="flex items-center justify-end border-t border-border bg-muted/40 p-4">
            <Button onClick={handoff} disabled={phase === 'thinking' || phase === 'typing'}>
              <CornerDownRight className="mr-1 size-4" />
              {phase === 'thinking' || phase === 'typing' ? 'Агент работает…' : 'Отдать агенту'}
            </Button>
          </div>
        </div>
      </div>
      <ScrollNav dir="down" label="Листайте вниз" className="absolute inset-x-0 bottom-6 z-20 mx-auto w-fit" />
    </div>
  );
}
```

- [ ] **Step 4: Запустить — проходит**

Run: `npx nx test landing`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add apps/landing/src/components/demo-section.tsx apps/landing/src/components/demo-section.spec.tsx
git commit -m "feat(landing): interactive agent demo card"
```

---

## Task 10: Карточка 4 — Хаб интеграций (AnimatedBeam)

**Files:**
- Create: `apps/landing/src/components/animated-beam.tsx` (порт)
- Create: `apps/landing/src/components/integrations-card.tsx`
- Test: `apps/landing/src/components/integrations-card.spec.tsx`

- [ ] **Step 1: Скопировать `animated-beam.tsx` из референса**

Скопировать `/Users/dmitrijordin/ai-reviews-landing/src/components/magicui/animated-beam.tsx` → `apps/landing/src/components/animated-beam.tsx`. Удалить `"use client";`. Импорты (`react-use-measure`, `motion/react`, `@/lib/utils`) — без изменений. Если внутри импорт `@/lib/utils` — уже есть (Task 3).

- [ ] **Step 2: Написать падающий тест**

`apps/landing/src/components/integrations-card.spec.tsx`:

```tsx
import { render, screen } from '@testing-library/react';
import { IntegrationsCard } from './integrations-card';

describe('IntegrationsCard', () => {
  it('показывает заголовок и ключевые узлы интеграций', () => {
    render(<IntegrationsCard />);
    expect(screen.getByRole('heading', { level: 2 }).textContent).toMatch(/откуда угодно/i);
    for (const node of ['Claude', 'MCP', 'REST API', 'n8n']) {
      expect(screen.getByText(node)).toBeTruthy();
    }
  });
});
```

- [ ] **Step 3: Запустить — падает**

Run: `npx nx test landing`
Expected: FAIL.

- [ ] **Step 4: Создать `integrations-card.tsx`**

```tsx
import { Fragment, forwardRef, useRef } from 'react';
import { Bot, Send, Wrench, Server, Workflow, Radio, KeyRound, GitMerge } from 'lucide-react';
import { cn } from '@/lib/utils';
import { AnimatedBeam } from '@/components/animated-beam';
import { ScrollNav } from '@/components/scroll-nav';

const Circle = forwardRef<HTMLDivElement, { className?: string; children?: React.ReactNode }>(
  ({ className, children }, ref) => (
    <div ref={ref} className={cn('z-10 flex size-14 items-center justify-center border border-border bg-background shadow-sm', className)}>
      {children}
    </div>
  ),
);
Circle.displayName = 'Circle';

function Node({ label, refProp, children }: { label: string; refProp: React.RefObject<HTMLDivElement | null>; children: React.ReactNode }) {
  return (
    <div className="flex flex-col items-center gap-2">
      <Circle ref={refProp}>{children}</Circle>
      <span className="text-xs font-medium text-muted-foreground">{label}</span>
    </div>
  );
}

export function IntegrationsCard() {
  const container = useRef<HTMLDivElement>(null);
  const center = useRef<HTMLDivElement>(null);
  const claude = useRef<HTMLDivElement>(null);
  const tg = useRef<HTMLDivElement>(null);
  const mcp = useRef<HTMLDivElement>(null);
  const rest = useRef<HTMLDivElement>(null);
  const n8n = useRef<HTMLDivElement>(null);
  const ws = useRef<HTMLDivElement>(null);

  const left = [claude, tg, mcp];
  const right = [rest, n8n, ws];

  return (
    <div id="integrations" className="relative flex h-full w-full flex-col items-center justify-center overflow-hidden bg-card px-6 py-16">
      <ScrollNav dir="up" label="Назад" className="absolute inset-x-0 top-24 z-20 mx-auto w-fit" />
      <div data-fade-on-scroll className="mb-10 max-w-xl text-center">
        <h2 className="text-balance font-heading text-3xl font-semibold tracking-tight md:text-4xl">
          Твой агент работает с доской откуда угодно
        </h2>
        <p className="mt-3 text-balance text-muted-foreground">
          MCP-тулы и REST — из Claude, Telegram или своих скриптов. Токены со скоупом, живое
          обновление, каждый шаг в трейсе.
        </p>
      </div>

      <div ref={container} data-fade-on-scroll className="relative flex h-[360px] w-full max-w-2xl items-stretch justify-between px-2">
        <div className="flex flex-col justify-around py-4">
          <Node label="Claude" refProp={claude}><Bot className="size-6 text-primary" /></Node>
          <Node label="OpenClaw (Telegram)" refProp={tg}><Send className="size-6 text-sky-500" /></Node>
          <Node label="MCP" refProp={mcp}><Wrench className="size-6 text-amber-500" /></Node>
        </div>

        <div className="flex flex-col items-center justify-center">
          <div ref={center} className="z-10 flex size-20 flex-col items-center justify-center bg-primary text-primary-foreground shadow-xl">
            <GitMerge className="size-6" />
          </div>
          <span className="mt-2 text-sm font-semibold">moongatracker</span>
        </div>

        <div className="flex flex-col justify-around py-4">
          <Node label="REST API" refProp={rest}><Server className="size-6 text-emerald-500" /></Node>
          <Node label="n8n" refProp={n8n}><Workflow className="size-6 text-rose-500" /></Node>
          <Node label="Socket.IO" refProp={ws}><Radio className="size-6 text-violet-500" /></Node>
        </div>

        {[...left, ...right].map((r, idx) => {
          const i = idx % 3;
          const curvature = i === 1 ? 0 : (i - 1) * 70;
          return (
            <Fragment key={idx}>
              <AnimatedBeam containerRef={container} fromRef={r} toRef={center} curvature={curvature} duration={2.5} delay={idx * 0.15} />
              <AnimatedBeam containerRef={container} fromRef={r} toRef={center} curvature={curvature} duration={2.5} delay={idx * 0.15 + 1.25} reverse />
            </Fragment>
          );
        })}
      </div>

      <div className="mt-8 flex items-center gap-2 text-xs text-muted-foreground">
        <KeyRound className="size-3.5" /> API-токены со скоупом и отзывом · трейс и откат каждого действия
      </div>
      <ScrollNav dir="down" label="Листайте вниз" className="absolute inset-x-0 bottom-6 z-20 mx-auto w-fit" />
    </div>
  );
}
```

- [ ] **Step 5: Запустить — проходит**

Run: `npx nx test landing`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add apps/landing/src/components/animated-beam.tsx apps/landing/src/components/integrations-card.tsx apps/landing/src/components/integrations-card.spec.tsx
git commit -m "feat(landing): integrations hub card + animated-beam"
```

---

## Task 11: Карточка 5 — Тарифы / финальный CTA

**Files:**
- Create: `apps/landing/src/components/pricing-section.tsx`
- Test: `apps/landing/src/components/pricing-section.spec.tsx`

- [ ] **Step 1: Написать падающий тест**

`apps/landing/src/components/pricing-section.spec.tsx`:

```tsx
import { render, screen } from '@testing-library/react';
import { PricingCard } from './pricing-section';

describe('PricingCard', () => {
  it('показывает три тарифа и финальный CTA на регистрацию', () => {
    render(<PricingCard />);
    for (const plan of ['Free', 'Pro', 'Team']) {
      expect(screen.getByText(plan)).toBeTruthy();
    }
    const cta = screen.getByRole('link', { name: /создать аккаунт/i });
    expect(cta.getAttribute('href')).toContain('/register');
  });
});
```

- [ ] **Step 2: Запустить — падает**

Run: `npx nx test landing`
Expected: FAIL.

- [ ] **Step 3: Создать `pricing-section.tsx`**

```tsx
import { Check } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { ScrollNav } from '@/components/scroll-nav';
import { APP_URL } from '@/lib/constants';

const PLANS = [
  { name: 'Free', tagline: 'Для одного проекта', features: ['1 доска', '1 агент-токен', 'История активности 7 дней'] },
  { name: 'Pro', tagline: 'Для активной работы с агентом', features: ['Безлимит досок и токенов', 'Полный трейс и откат', 'Приоритетный realtime'], highlight: true },
  { name: 'Team', tagline: 'Для команды', features: ['Роли и участники', 'Общие доски', 'Поддержка (SSO — позже)'] },
];

export function PricingCard() {
  return (
    <div id="pricing" className="relative flex h-full w-full items-center overflow-y-auto bg-background px-6 py-16">
      <ScrollNav dir="up" label="Назад" className="absolute inset-x-0 top-24 z-20 mx-auto w-fit" />
      <div data-fade-on-scroll className="mx-auto w-full max-w-4xl">
        <div className="mx-auto mb-10 max-w-xl text-center">
          <h2 className="text-balance font-heading text-3xl font-semibold tracking-tight md:text-4xl">Начни бесплатно</h2>
          <p className="mt-3 text-balance text-muted-foreground">Доска, где человек и агент работают вместе. Апгрейд — когда вырастешь.</p>
        </div>

        <div className="grid gap-4 sm:grid-cols-3">
          {PLANS.map((p) => (
            <div key={p.name} className={`flex flex-col border p-6 ${p.highlight ? 'border-primary' : 'border-border'}`}>
              <div className="font-heading text-lg font-semibold">{p.name}</div>
              <div className="mt-1 text-sm text-muted-foreground">{p.tagline}</div>
              <ul className="mt-4 flex flex-1 flex-col gap-2 text-sm">
                {p.features.map((f) => (
                  <li key={f} className="flex items-start gap-2">
                    <Check className="mt-0.5 size-4 shrink-0 text-primary" /> <span>{f}</span>
                  </li>
                ))}
              </ul>
              <Button asChild variant={p.highlight ? 'default' : 'outline'} className="mt-6">
                <a href={APP_URL}>Начать</a>
              </Button>
            </div>
          ))}
        </div>

        <div className="mt-10 text-center">
          <Button asChild size="lg" className="h-12 px-6 text-base">
            <a href={APP_URL}>Создать аккаунт</a>
          </Button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Запустить — проходит**

Run: `npx nx test landing`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add apps/landing/src/components/pricing-section.tsx apps/landing/src/components/pricing-section.spec.tsx
git commit -m "feat(landing): pricing / final CTA card"
```

---

## Task 12: Сборка страницы + финальный smoke

**Files:**
- Modify: `apps/landing/src/app/app.tsx`
- Test: `apps/landing/src/app/app.spec.tsx`

- [ ] **Step 1: Написать тест композиции**

`apps/landing/src/app/app.spec.tsx`:

```tsx
import { render, screen } from '@testing-library/react';
import App from './app';

describe('App', () => {
  it('рендерит хедер и hero-заголовок', () => {
    render(<App />);
    expect(screen.getByRole('heading', { level: 1 }).textContent).toMatch(/полноправный участник/i);
    expect(screen.getAllByText(/moongatracker/i).length).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 2: Запустить — падает**

Run: `npx nx test landing`
Expected: FAIL (App ещё заглушка из Task 4).

- [ ] **Step 3: Собрать финальный `app.tsx`**

```tsx
import ReactLenis from 'lenis/react';
import { Header } from '@/components/header';
import { StickyCardStack } from '@/components/sticky-card-stack';
import { HeroCard } from '@/components/hero-section';
import { HowItWorksCard } from '@/components/how-it-works-section';
import { DemoCard } from '@/components/demo-section';
import { IntegrationsCard } from '@/components/integrations-card';
import { PricingCard } from '@/components/pricing-section';

export function App() {
  return (
    <>
      <Header />
      <ReactLenis root options={{ autoRaf: false, smoothWheel: false }}>
        <StickyCardStack>
          <HeroCard />
          <HowItWorksCard />
          <DemoCard />
          <IntegrationsCard />
          <PricingCard />
        </StickyCardStack>
      </ReactLenis>
    </>
  );
}

export default App;
```

- [ ] **Step 4: Запустить тест — проходит**

Run: `npx nx test landing`
Expected: PASS (весь набор тестов лендинга зелёный).

- [ ] **Step 5: Прогнать сборку и линт**

Run: `npx nx build landing && npx nx lint landing`
Expected: сборка успешна, линт без ошибок.

- [ ] **Step 6: Ручной smoke в dev**

Run: `npx nx serve landing`
Expected: 5 экранов переключаются одним жестом (Hero → Как работает → Демо → Интеграции → Тарифы); в демо «Отдать агенту» показывает печать комментария, лейблы, перенос `idea → разбор` и работает «Откатить»; лучи в хабе анимируются; CTA ведут на `/register`.

- [ ] **Step 7: Commit**

```bash
git add apps/landing/src/app/app.tsx apps/landing/src/app/app.spec.tsx
git commit -m "feat(landing): compose 5-card landing page"
```

---

## Self-Review (проверено при написании плана)

- **Покрытие спеки:** Hero (Task 6), Как работает (Task 7), Демо + логика (Tasks 8–9), Хаб интеграций (Task 10), Тарифы/CTA (Task 11), каркас смены карточек (Task 4), тема/шрифт/токены (Task 2), CTA→регистрация (`APP_URL`, Task 3 + все карточки). Нарратив A+B, трейс/откат — в демо (Task 9) и подписи хаба (Task 10). Self-hosted не упоминается нигде. ✅
- **Без заглушек:** весь код карточек приведён полностью; портируемые файлы указаны точным путём-источником + список правок. Цены тарифов намеренно опущены (решение спеки), это не placeholder-код. ✅
- **Согласованность типов:** `buildAgentRun`/`sizeOf`/`AgentRun`/`ColumnKey` из Task 8 используются в Task 9 с теми же именами; `APP_URL` из Task 3 — везде; `cn` — из Task 3. ✅
```
